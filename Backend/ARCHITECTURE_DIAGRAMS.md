# SRMS System Architecture & Diagrams

## 🏗️ System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                      CLIENT LAYER (Angular)                         │
│                    (localhost:4200)                                  │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  Login Page  │  Admin Dashboard  │  Teacher Portal  │ Student│   │
│  └──────────────────────────────────────────────────────────────┘   │
│                            ↓ (HTTP/JSON)                            │
│                      CORS Enabled ✓                                 │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                    API GATEWAY LAYER                                │
│                   (Spring Boot: 8080/api)                           │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  /api/auth/*       → AuthController                          │   │
│  │  /api/students/*   → StudentController                       │   │
│  │  /api/marks/*      → MarksController                         │   │
│  │  /api/recheck-*    → RecheckRequestController                │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                   BUSINESS LOGIC LAYER (Services)                   │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  UserService       → Authentication & User Management        │   │
│  │  StudentService    → Student CRUD Operations                │   │
│  │  MarksService      → Marks Management                        │   │
│  │  RecheckRequest*   → Recheck Request Processing              │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                  DATA ACCESS LAYER (Repositories)                   │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  UserRepository              → JPA Operations                │   │
│  │  StudentRepository           → JPA Operations                │   │
│  │  MarksRepository             → JPA Operations                │   │
│  │  RecheckRequestRepository    → JPA Operations                │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                  DATABASE LAYER (MySQL)                             │
│                (localhost:3306/srms_db)                             │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  ┌────────────┐  ┌───────────┐  ┌────────┐  ┌────────────┐  │   │
│  │  │   users    │  │  student  │  │ marks  │  │  recheck_  │  │   │
│  │  │  (26 rows) │  │ (20 rows) │  │(120)   │  │ request(5) │  │   │
│  │  └────────────┘  └───────────┘  └────────┘  └────────────┘  │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│                    LOGIN PROCESS FLOW                            │
└──────────────────────────────────────────────────────────────────┘

USER INTERFACE (Angular)
        ↓
     [Email: admin@gmail.com]
     [Password: 123456]
        ↓
   POST /api/auth/login
   (HTTP Request with JSON)
        ↓
┌──────────────────────────────────────────────────────────────────┐
│             SPRING BOOT BACKEND (8080)                           │
│                                                                  │
│  AuthController.login(LoginRequest)                             │
│       ↓                                                          │
│  Validate Input                                                 │
│  ├─ Email not empty?     ✓                                     │
│  ├─ Password not empty?  ✓                                     │
│       ↓                                                          │
│  UserService.authenticateUser(email, password)                 │
│       ↓                                                          │
│  UserRepository.findByEmailAndPassword()                        │
│       ↓                                                          │
│  Query MySQL Database                                           │
│  ├─ Find user with email = 'admin@gmail.com'                   │
│  ├─ Verify password = '123456'                                 │
│  ├─ Check is_active = true                                     │
│       ↓                                                          │
│  If Found:                                                      │
│  ├─ Create LoginResponse                                        │
│  ├─ Set userId, email, role                                    │
│  ├─ Return 200 OK with ApiResponse wrapper                     │
│       ↓                                                          │
│  If Not Found:                                                  │
│  ├─ Return 401 Unauthorized                                     │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
        ↓
   HTTP 200 Response
   {
     "status": 200,
     "message": "Login successful",
     "data": {
       "userId": 1,
       "email": "admin@gmail.com",
       "role": "admin"
     }
   }
        ↓
   FRONTEND
   ├─ Save to localStorage
   ├─ Store userId & role
   └─ Redirect to /admin
```

---

## 🗄️ Database Schema Diagram

```
┌─────────────────────────┐
│        USERS            │
├─────────────────────────┤
│ PK: user_id (BIGINT)    │
│ email (VARCHAR, UNIQUE) │◄──┐
│ password (VARCHAR)      │   │ User Relationship
│ role (VARCHAR)          │   │
│ is_active (BOOLEAN)     │   │
│ created_at (TIMESTAMP)  │   │
│ updated_at (TIMESTAMP)  │   │
└─────────────────────────┘   │
                               │
┌─────────────────────────┐   │
│       STUDENT           │   │
├─────────────────────────┤   │
│ PK: id (BIGINT)         │   │
│ name (VARCHAR)          │───┤─ One User → One Student
│ email (VARCHAR, UNIQUE) │───┘
│ roll_no (VARCHAR, UNQ)  │
│ class_name (VARCHAR)    │
│ date_of_birth (VARCHAR) │
│ phone (VARCHAR)         │
│ address (VARCHAR)       │
│ is_active (BOOLEAN)     │
│ timestamps              │
└─────────────────────────┘
         ▲
         │ Student ID (FK)
         │
┌─────────────────────────┐
│        MARKS            │
├─────────────────────────┤
│ PK: id (BIGINT)         │
│ FK: student_id (BIGINT) │
│ subject (VARCHAR)       │
│ marks_obtained (INT)    │
│ max_marks (INT)         │
│ term (VARCHAR)          │
│ year (INT)              │
│ timestamps              │
└─────────────────────────┘

┌─────────────────────────┐
│   RECHECK_REQUEST       │
├─────────────────────────┤
│ PK: id (BIGINT)         │
│ FK: student_id (BIGINT) │
│ subject (VARCHAR)       │
│ reason (VARCHAR)        │
│ status (VARCHAR)        │
│ old_marks (INT)         │
│ new_marks (INT)         │
│ admin_comments (VARCHAR)│
│ timestamps              │
└─────────────────────────┘
```

---

## 👥 User Role Hierarchy

```
┌─────────────────────────────────────┐
│        SYSTEM ADMIN (1)             │
├─────────────────────────────────────┤
│ Email: admin@gmail.com              │
│ Password: 123456                    │
│                                     │
│ Permissions:                        │
│ ✓ Manage all users                  │
│ ✓ Create/Update/Delete students     │
│ ✓ Create/Update teachers            │
│ ✓ View all marks                    │
│ ✓ Approve recheck requests          │
│ ✓ Generate reports                  │
│ ✓ System configuration              │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│        TEACHERS (5)                 │
├─────────────────────────────────────┤
│ Email Examples:                     │
│ - rahul@gmail.com                   │
│ - ananya@gmail.com                  │
│ - sanjay@gmail.com                  │
│ - priya@gmail.com                   │
│ - vikram@gmail.com                  │
│ Password: 123456 (all)              │
│                                     │
│ Permissions:                        │
│ ✓ View assigned students            │
│ ✓ Add/Update student marks          │
│ ✓ View recheck requests             │
│ ✓ Comment on rechecks               │
│ ✓ View own profile                  │
│ ✗ Create/Delete students            │
│ ✗ Approve rechecks                  │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│        STUDENTS (20)                │
├─────────────────────────────────────┤
│ Class 1: john, alice, bob, ...      │
│ Class 2: emma, frank, grace, ...    │
│ Class 3: jack, kate, leo, ...       │
│ Class 4: olivia, paul, quinn, ...   │
│ Password: 123456 (all)              │
│                                     │
│ Permissions:                        │
│ ✓ View own marks                    │
│ ✓ View own profile                  │
│ ✓ Request recheck                   │
│ ✓ Track recheck status              │
│ ✓ View results                      │
│ ✗ Modify any marks                  │
│ ✗ View other students' data         │
│ ✗ Approve rechecks                  │
└─────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### Login Data Flow
```
Angular Frontend                Backend                 Database
      │                           │                       │
      ├──────────────────────────>│ POST /auth/login       │
      │                           │                       │
      │                           ├──────────────────────>│ SELECT * FROM users
      │                           │   WHERE email=?       │
      │                           │   AND password=?      │
      │                           │                       │
      │                           │<──────────────────────┤ User object
      │                           │                       │
      │<──────────────────────────┤ 200 OK + LoginResponse│
      │   { userId, role, ... }   │                       │
      │                           │                       │
      ├─ Save to localStorage    │                       │
      ├─ Route to dashboard      │                       │
```

### Create Marks Data Flow
```
Angular Frontend                Backend                 Database
      │                           │                       │
      ├──────────────────────────>│ POST /marks           │
      │  { studentId, subject,   │                       │
      │    marks, term, year }   │                       │
      │                           │                       │
      │                           ├──────────────────────>│ INSERT INTO marks
      │                           │                       │ VALUES(...)
      │                           │                       │
      │                           │<──────────────────────┤ Success
      │                           │                       │
      │<──────────────────────────┤ 201 Created          │
      │   { id, data, ... }       │                       │
      │                           │                       │
      ├─ Show success message    │                       │
      └─ Refresh marks list      │                       │
```

---

## 📊 Database Data Statistics

```
┌──────────────────────────────────────┐
│      USERS TABLE (26 Records)        │
├──────────────────────────────────────┤
│ Role        │ Count │ Email Pattern  │
├─────────────┼───────┼────────────────┤
│ admin       │   1   │ admin@...      │
│ teacher     │   5   │ name@...       │
│ student     │  20   │ name@...       │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│     STUDENT TABLE (20 Records)       │
├──────────────────────────────────────┤
│ Class    │ Count │ Roll No Range  │
├──────────┼───────┼────────────────┤
│ Class 1  │   5   │ 101-105        │
│ Class 2  │   5   │ 201-205        │
│ Class 3  │   5   │ 301-305        │
│ Class 4  │   5   │ 401-405        │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│      MARKS TABLE (120 Records)       │
├──────────────────────────────────────┤
│ Subject           │ Count  │ Avg     │
├───────────────────┼────────┼─────────┤
│ Mathematics       │  20    │  87.5   │
│ English           │  20    │  87.0   │
│ Science           │  20    │  86.8   │
│ History           │  20    │  86.5   │
│ Geography         │  20    │  86.7   │
│ Physical Education│  20    │  90.5   │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│   RECHECK_REQUEST TABLE (5 Records)  │
├──────────────────────────────────────┤
│ Status     │ Count  │ Examples       │
├────────────┼────────┼────────────────┤
│ pending    │  2     │ Awaiting review│
│ approved   │  1     │ Recount done   │
│ rejected   │  1     │ Denied         │
│ completed  │  1     │ Finished       │
└──────────────────────────────────────┘
```

---

## 🔌 API Request/Response Cycle

```
CLIENT                                      SERVER
  │                                          │
  ├─ HTTP Request                           │
  │  {                                      │
  │    "email": "admin@gmail.com",          │
  │    "password": "123456"                 │
  │  }                                      │
  ├─────────────────────────────────────┬──>│
  │    POST /api/auth/login             │  │
  │                                    │  │
  │                        AuthController │
  │                                    │  ├─ Validate input
  │                                    │  ├─ Call UserService
  │                                    │  ├─ Query database
  │                                    │  ├─ Create response
  │                                    │  │
  │  ┌─────────────────────────────────┤<─┤
  │  │ HTTP 200 Response               │  │
  │  │ {                               │  │
  │  │   "status": 200,                │  │
  │  │   "message": "Login success",   │  │
  │  │   "data": {                     │  │
  │  │     "userId": 1,                │  │
  │  │     "email": "admin@gmail.com", │  │
  │  │     "role": "admin"             │  │
  │  │   },                            │  │
  │  │   "error": null                 │  │
  │  │ }                               │  │
  │  │                                │  │
  │  ├─ Extract user data             │  │
  │  ├─ Save to localStorage          │  │
  │  ├─ Redirect to dashboard         │  │
  │  └─ Load admin components         │  │
```

---

## 🎯 Request/Response Examples

### Login Request
```json
POST /api/auth/login
Content-Type: application/json

{
  "email": "john@gmail.com",
  "password": "123456"
}
```

### Login Response (Success - 200)
```json
{
  "status": 200,
  "message": "Login successful",
  "data": {
    "userId": 21,
    "email": "john@gmail.com",
    "role": "student",
    "message": "Login successful",
    "success": true
  },
  "error": null
}
```

### Login Response (Failure - 401)
```json
{
  "status": 401,
  "message": "Invalid email or password",
  "data": null,
  "error": "Invalid email or password"
}
```

### Register Request
```json
POST /api/auth/register
Content-Type: application/json

{
  "email": "newteacher@gmail.com",
  "password": "newpass123",
  "role": "teacher",
  "isActive": true
}
```

### Register Response (201)
```json
{
  "status": 201,
  "message": "User registered successfully",
  "data": {
    "userId": 27,
    "email": "newteacher@gmail.com",
    "password": "newpass123",
    "role": "teacher",
    "isActive": true,
    "createdAt": "2024-12-19T10:30:00",
    "updatedAt": "2024-12-19T10:30:00"
  },
  "error": null
}
```

---

## 🔄 Complete API Endpoint Map

```
┌─────────────────────────────────────────────┐
│        AUTHENTICATION ENDPOINTS             │
├──────────────────────┬──────────────────────┤
│ POST   /auth/login   │ User login           │
│ POST   /auth/register│ Create new user      │
│ POST   /auth/logout  │ Logout user          │
│ GET    /auth/verify  │ Check user exists    │
│ GET    /auth/user/:id│ Get user by ID       │
│ PUT    /auth/user/:id│ Update user          │
│ GET    /auth/check   │ Check email avail.   │
└──────────────────────┴──────────────────────┘

┌──────────────────────────────────────────────┐
│        STUDENT ENDPOINTS                    │
├──────────────────────┬──────────────────────┤
│ GET    /students/all │ List all students    │
│ GET    /students/:id │ Get student by ID    │
│ POST   /students     │ Create student       │
│ PUT    /students/:id │ Update student       │
│ DELETE /students/:id │ Delete student       │
│ GET    /students/... │ Get by email/class   │
└──────────────────────┴──────────────────────┘

┌──────────────────────────────────────────────┐
│         MARKS ENDPOINTS                     │
├──────────────────────┬──────────────────────┤
│ GET    /marks/all    │ List all marks       │
│ GET    /marks/:id    │ Get marks by ID      │
│ POST   /marks        │ Create marks record  │
│ PUT    /marks/:id    │ Update marks         │
│ DELETE /marks/:id    │ Delete marks         │
│ GET    /marks/...    │ Filter by student/.. │
└──────────────────────┴──────────────────────┘

┌──────────────────────────────────────────────┐
│    RECHECK REQUEST ENDPOINTS                │
├──────────────────────┬──────────────────────┤
│ GET    /recheck-req  │ List all requests    │
│ GET    /recheck-req/:id│ Get request        │
│ POST   /recheck-req  │ Create request       │
│ PUT    /recheck-req/:id│ Update status      │
│ DELETE /recheck-req/:id│ Delete request     │
│ GET    /recheck-req/...│ Filter by status   │
└──────────────────────┴──────────────────────┘
```

---

## 📱 Frontend-Backend Communication

```
┌─────────────────────────────────────────────────────────┐
│          FRONTEND LAYER (Angular)                       │
│                   localhost:4200                        │
│                                                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Components                                      │   │
│  │  ├─ LoginComponent                               │   │
│  │  ├─ AdminDashboardComponent                      │   │
│  │  ├─ TeacherDashboardComponent                    │   │
│  │  ├─ StudentDashboardComponent                    │   │
│  │  └─ Other Components                             │   │
│  │                                                  │   │
│  │  ┌──────────────────────────────────────────┐   │   │
│  │  │ Services                                 │   │   │
│  │  │ ├─ AuthService (Updated)                │   │   │
│  │  │ ├─ StudentService                       │   │   │
│  │  │ ├─ MarksService                         │   │   │
│  │  │ ├─ RecheckService                       │   │   │
│  │  │ └─ HttpInterceptor                      │   │   │
│  │  └──────────────────────────────────────────┘   │   │
│  │                     ↓                           │   │
│  │  ┌──────────────────────────────────────────┐   │   │
│  │  │ HttpClientModule                         │   │   │
│  │  │ (Makes HTTP calls to backend)            │   │   │
│  │  └──────────────────────────────────────────┘   │   │
│  │                                                  │   │
│  └──────────────────────────────────────────────────┘   │
│                     ↓ HTTP/REST                        │
│              CORS Enabled ✓                            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│        BACKEND LAYER (Spring Boot)                      │
│                localhost:8080/api                       │
│                                                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Controllers                                     │   │
│  │  ├─ AuthController (NEW)                        │   │
│  │  ├─ StudentController                           │   │
│  │  ├─ MarksController                             │   │
│  │  └─ RecheckRequestController                    │   │
│  │                                                  │   │
│  │  ┌──────────────────────────────────────────┐   │   │
│  │  │ Services                                 │   │   │
│  │  │ ├─ UserService (NEW)                    │   │   │
│  │  │ ├─ StudentService                       │   │   │
│  │  │ ├─ MarksService                         │   │   │
│  │  │ └─ RecheckRequestService                │   │   │
│  │  └──────────────────────────────────────────┘   │   │
│  │                                                  │   │
│  │  ┌──────────────────────────────────────────┐   │   │
│  │  │ Repositories                             │   │   │
│  │  │ ├─ UserRepository                        │   │   │
│  │  │ ├─ StudentRepository                     │   │   │
│  │  │ ├─ MarksRepository                       │   │   │
│  │  │ └─ RecheckRequestRepository              │   │   │
│  │  └──────────────────────────────────────────┘   │   │
│  │                     ↓                           │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
                     ↓ JDBC
┌─────────────────────────────────────────────────────────┐
│        DATABASE LAYER (MySQL)                           │
│              localhost:3306                             │
│              srms_db                                    │
│                                                         │
│  ┌────────────┐ ┌──────────┐ ┌─────────┐ ┌─────────┐  │
│  │   users    │ │ student  │ │  marks  │ │recheck_ │  │
│  │            │ │          │ │         │ │request  │  │
│  │ 26 records │ │20 records│ │120 rec. │ │ 5 rec.  │  │
│  └────────────┘ └──────────┘ └─────────┘ └─────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🎓 Summary Diagram

```
┌─────────────────────────────────────────────────────┐
│                   COMPLETE FLOW                     │
├─────────────────────────────────────────────────────┤
│                                                     │
│ User Opens App (localhost:4200)                     │
│        ↓                                            │
│ Sees Login Page                                     │
│        ↓                                            │
│ Enters Credentials (e.g., admin@gmail.com)         │
│        ↓                                            │
│ AuthService calls /api/auth/login                  │
│        ↓                                            │
│ Backend processes request                          │
│ ├─ AuthController receives POST                    │
│ ├─ UserService authenticates                       │
│ ├─ Queries MySQL database                          │
│ └─ Returns LoginResponse                           │
│        ↓                                            │
│ Frontend saves userId & role to localStorage       │
│        ↓                                            │
│ Routes to appropriate dashboard                    │
│ ├─ admin → /admin (AdminComponent)                │
│ ├─ teacher → /teacher (TeacherComponent)          │
│ └─ student → /student (StudentComponent)          │
│        ↓                                            │
│ User can now:                                       │
│ ├─ View/Manage data based on role                  │
│ ├─ Make API calls with authorization               │
│ └─ Access protected routes                         │
│        ↓                                            │
│ Backend processes requests using JWT/Token         │
│        ↓                                            │
│ Database returns filtered data                     │
│        ↓                                            │
│ Frontend displays results to user                  │
│        ↓                                            │
│ User logs out → localStorage cleared               │
│        ↓                                            │
│ Redirects back to login page                       │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

This diagram document provides visual representation of your complete SRMS system architecture!
